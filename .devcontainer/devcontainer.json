{
	"name": "DevContainer (can be used for contributing to kubify core)",
	"dockerComposeFile": [
		"../docker-compose.yml",
		"docker-compose.yml"
	],
	"service": "kubify",
	"workspaceFolder": "/src/kubify",
	"postStartCommand": "make clean && make lint && make help && make security && make package && make coverage && make test" // "kubify up"
}